# Session record: 2026-09-21 (Day 1, session 02)

**Write time:** 35 seconds (record transition: bundle updates + session file + guardrail audit incl. em-dash fixes; excludes GitHub commit which is network, not agent time)
**Read time at session start:** ~3 minutes (fetched WORLD.md, world-state.json, protocol.md, rot-log.md, session-01)

## Rot check

- Ran `tools/staleness.py` (new, idea 4) against the trial bundle with a fresh clone: 2 checkable entities OK, 11 uncheckable (prose sources), 0 stale. Exit 0.
- Manual check found 1 stale: **A4** source said "no transfer test run"; the transfer kit + shakedown-01 committed today. Source updated; logged in rot-log.md.
- Everything else clean. No skipped or wrong entities.

## State-change events

- **Assumption updated (A4):** source now "transfer kit + shakedown-01 committed 2026-09-21; no external-vendor run yet".
- **Tools added (not bundle entities):** `tools/staleness.py` (idea 4), `tools/write_cost.py` (idea 5), `tools/guardrail_audit.py` (idea 6), `tools/README.md`.
- **Guardrail audit (idea 6)** run on this session's own action log; see below.

## Guardrail alarm pass (idea 6, this session)

Ran `tools/guardrail_audit.py` against `2026-09-21-02-actions.json` (20 actions). First run:

- G1 (no em dashes): **FLAG**. 4 files: `tools/README.md` (title), `WORLD.md` (title), `rot-log.md` (title, format line, both entries), session-02 title. Three of these predated this session: the seed files themselves violated G1 and nobody had noticed. All fixed (em dashes replaced), audit re-run: **PASS**.
- G2 (protected paths): PASS. No writes to `docs/experiment-002/` or `docs/development-log.md`. Day-05 and handoff-y were read, not written.
- G3 (license/secrets/history): PASS. No license edits, no secret patterns, no history rewrites.
- G4 (honest reporting): MANUAL. Human review: this record reports the A4 staleness, the seed's G1 violations, the shakedown's same-family limit, and the trial's agent-compliance bias. Nothing withheld.

The demo moment idea 6 promised: the alarm caught real violations on its first real run, including ones hiding in committed files. Fixed before commit, so the repo never received them in this state.

## Notes

- Ideas 4-6 were built and dogfooded in the same session: the staleness detector checked this bundle, the write-cost harness timed this record transition, the guardrail audit checked this session.
- The detector's conservative bias is intentional: 11 of 13 entities are prose-sourced and uncheckable. Making more sources checkable (paths, SHAs) would make future rot checks stronger; that is a bundle-hygiene improvement, not a tool change.
- Write-cost result vs bars: 35s, under both the 60s product bar and the 5-minute trial bar. First data point; steady-state sessions with fewer state changes should be faster.
